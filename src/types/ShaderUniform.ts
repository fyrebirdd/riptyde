export class ShaderUniformGroup{

    private uniformEntries: UniformData[];

    constructor(id: number, ){
        this.uniformEntries = [];
    }

    public AddUniform(uniformData:UniformData){

    }
}

export class UniformData{

    constructor(shaderVisibility:GPUShaderStageFlags, bufferType:GPUBufferBindingType, ){

    }
}

